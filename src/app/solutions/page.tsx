import Link from 'next/link';
import { solutions } from '@/lib/redesign';
import { Hero, Section, Heading, Flow, TextLink, Cards, Closing } from '@/components/studio/Shared';
import { pageMetadata } from '@/lib/seo';
export const metadata = pageMetadata({ title: 'Solutions for Your Business', description: 'Explore practical AI workflows and website design. Start with enquiries, documents, operations, knowledge, reporting or your brand website.', path: '/solutions' });
export default function Solutions() { return <><Hero label="Start with an outcome" title="What would you like to work better?" body="Choose a familiar problem. See what a useful system could do, where your team stays involved and which service can help." primary="Find your starting point" href="#solutions"/><Section light id="solutions"><Heading title="Recognise the work. Find a way forward."/><nav className="anchor-links" aria-label="Solution types">{solutions.map(s => <Link href={`#${s.id}`} key={s.id}>{s.label}</Link>)}</nav></Section>{solutions.map((s, i) => <Section key={s.id} id={s.id} light={i % 2 === 0}><div className="split solution-intro"><div><p className="eyebrow">0{i + 1} / {s.label}</p><h2>{s.name}</h2></div><div><p className="lede">{s.body}</p><TextLink href={`/systems/${s.service}`}>Explore the service</TextLink></div></div><Flow steps={s.steps.map((step, n) => [step, n === 3 ? 'Your rules and review' : 'Context carried to the next step'])} gate={3} note={s.gate}/></Section>)}<Section light><Heading title="More than an answer on a screen."/><Cards items={[["A working experience", "The tools and interface needed to complete the job."], ["The rules around it", "Clear boundaries, permissions and review decisions."], ["Someone accountable", "Ownership for content, changes and the next step."]]}/></Section><Closing /></>; }
