import type { MetadataRoute } from 'next';
import { services, articles } from '@/lib/redesign';
import { collections, guides } from '@/lib/insights';
import { siteUrl } from '@/lib/seo';
export default function sitemap(): MetadataRoute.Sitemap { return [...['/', '/solutions', '/systems', '/blueprint', '/how-we-work', '/industries', '/about', '/insights', '/apply'].map(path => ({ url: siteUrl(path), changeFrequency: 'monthly' as const, priority: path === '/' ? 1 : 0.8 })), ...services.map(s => ({ url: siteUrl(`/systems/${s.slug}`), changeFrequency: 'monthly' as const, priority: 0.8 })), ...collections.map(c => ({ url: siteUrl(`/insights/topics/${c.id}`), changeFrequency: 'weekly' as const, priority: 0.7 })), ...guides.map(g => ({ url: siteUrl(`/insights/${g.slug}`), lastModified: new Date(g.publishedAt), changeFrequency: 'monthly' as const, priority: 0.7 })), ...articles.map(a => ({ url: siteUrl(`/insights/${a.slug}`), lastModified: new Date(a.date), changeFrequency: 'monthly' as const, priority: 0.7 }))]; }
